<Window MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonDown="Window_MouseRightButtonDown"
        MouseMove="Window_MouseMove" x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        xmlns:mini="clr-namespace:MiniEditor;assembly=MiniEditorData"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
  <Window.Resources>
    <DataTemplate DataType="mini:IFigure">
      <Label Content="{Binding Name}"/>
    </DataTemplate>
  </Window.Resources>
  <StackPanel  Height="580" >
    <Canvas  Width="800" Height="22" RenderTransformOrigin="0.598,0.537" >
      <Rectangle Fill="LightBlue" Canvas.Left="0" Canvas.Top="0" Width="800" Height="22" />
      <Button Content="Add" Command="{Binding Add}" Width="55"  />
      <Button Content="Delete" Command="{Binding Delete}" Width="55" Canvas.Left="55"/>
      <Button Content="SaveAll" Command="{Binding SaveAll}" Width="55" Canvas.Left="110"/>
      <Button Content="LoadAll" Command="{Binding LoadAll}" Width="55" Canvas.Left="165"/>
      <Button Content="Circle" Command="{Binding LoadAll}" Width="55" Canvas.Left="250"/>
      <Button Content="Line" Command="{Binding LoadAll}" Width="55" Canvas.Left="300"/>
      <Button Content="Triangle" Command="{Binding LoadAll}" Width="55" Canvas.Left="350"/>
      <Button Content="Polygon" Command="{Binding LoadAll}" Width="55" Canvas.Left="400"/>
      <TextBox x:Name="Errorbox" Text="Hello, world!" MaxLength ="100" Canvas.Left="490" Canvas.Top="1" Width="300" />
      <Label x:Name="NumberOfFigures" Content="0" Canvas.Left="230" Canvas.Top="-4" Height="27" Width="29"/>
      <Label x:Name="Error" Content="" Canvas.Left="479" Canvas.Top="-4" Height="27" Width="311"/>
    </Canvas>
    <Canvas x:Name="MainCanvas" Width="800" Height="472"/>

    <ListView ItemsSource="{Binding ViewModel.AllFigures}"/>



  </StackPanel>
</Window>
